import {makeStyles} from '@material-ui/core/styles';

export const useStyles = makeStyles((theme) => ({
    ChainWrap:{
        padding: '50px',
        '& a': {
            color: '#fff'
        }
    }
}));
